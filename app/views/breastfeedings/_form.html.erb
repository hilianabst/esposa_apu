<%= form_with(model: breastfeeding, local: true) do |form| %>
  <% if breastfeeding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(breastfeeding.errors.count, "error") %> prohibited this breastfeeding from being saved:</h2>

      <ul>
      <% breastfeeding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, :id, :breastfeeding_quantity%>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.text_field :duration, :id, :breastfeeding_duration %>
  </div>

  <div class="field">
    <%= form.label :baby_id %>
    <%= form.select :baby_id options_for_select(@babies)%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
